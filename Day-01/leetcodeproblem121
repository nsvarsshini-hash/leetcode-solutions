class Solution:
    def maxProfit(self, prices):
        mp=0
        buy=prices[0]
        for i in range(len(prices)):
            if buy>prices[i]:
                buy=prices[i]
            cp=prices[i]-buy
            mp=max(mp,cp)
        return mp
